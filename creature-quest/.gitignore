node_modules/
dist/
*.local
.DS_Store

# Sprite/tile PNGs are build artifacts: AI versions are downloaded from the
# CDN (sprite-manifest.json) in CI, procedural fallbacks are generated by
# gen-sprites.mjs. Never commit them so they can't shadow the AI download.
src/assets/sprites/*.png
src/assets/tiles/*.png
