node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.local
.vite
npm-debug.log*

# Generated at build time by scripts/fetch-prices.mjs — but the card snapshot is
# committed and grown incrementally by the deploy workflow, so keep that one.
public/data/*
!public/data/cards.json
