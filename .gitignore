node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.local
.vite
npm-debug.log*

# Generated at build time by scripts/fetch-prices.mjs — but the card snapshots
# are committed and grown incrementally by the deploy workflow, so keep those.
public/data/*
!public/data/cards.json
!public/data/onepiece.json
!public/data/yugioh.json
!public/data/magic.json

# Rechnet artefakt.py aus km1-app/app/index.html — nur fuer die Vorschau auf
# claude.ai, keine Quelle.
km1-app/artefakt.html
km1-app/artefakt-apple.html
