# Supabase CLI config. Project ref set below — connect with:
#   supabase login && supabase link --project-ref svrmotfalixquiznrofi
# (Reference ID: Supabase Dashboard → Project Settings → General.)
project_id = "svrmotfalixquiznrofi"

# Edge Functions — JWT verification per function (so you don't need
# --no-verify-jwt on deploy):
#   • checkout/portal are called by the logged-in app → require a valid JWT.
#   • the Stripe webhook is called by Stripe (no Supabase JWT) → must be public.
#   • prices is a public read proxy → public.
[functions.create-checkout]
verify_jwt = true

[functions.customer-portal]
verify_jwt = true

[functions.stripe-webhook]
verify_jwt = false

[functions.prices]
verify_jwt = false
